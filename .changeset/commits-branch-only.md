---
"gitbash": minor
---

`commits` only lists the current branch's own commits (not in the base branch; first-parent history on the base branch itself), `--all` shows all recent commits. It offers to fast-forward first when the upstream has new commits.
