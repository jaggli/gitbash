---
"gitbash": minor
---

Show a colorful gitbash banner with the current version in `gitbash --help` (and `gitbash` without arguments), and color the section headings in the help of `gitbash` and all commands
