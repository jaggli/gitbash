---
"gitbash": patch
---

Windows: `gitbash --config` offers to load the gitbash functions in your PowerShell profiles (Windows PowerShell and PowerShell 7), creating the profile if needed, and to allow local scripts when PowerShell blocks them
