---
"gitbash": minor
---

Windows support: gitbash runs in Git Bash and WSL (npm and the install script), opens pull requests and repositories in the Windows browser, offers to install fzf, delta and bat with winget, and `gitbash --init --shell=pwsh` prints functions to use the commands from PowerShell
